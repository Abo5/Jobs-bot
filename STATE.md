# حالة بوت الوظائف — كما هو الآن

**تاريخ التوثيق:** 2026-08-12 · **المسار:** `/home/panda/Desktop/Jobs-bot`
**البيئة:** Raspberry Pi · Node v22.22.2 (nvm) · توقيت الجهاز `America/New_York`

هذا توثيق **as-is** لما يعمل عليه البوت فعلياً، مأخوذ من الكود والإعدادات
والسجلات لا من التصميم المقصود.

---

## ⚠️ تحذير عاجل — القرص ممتلئ ١٠٠٪

```
/dev/mmcblk0p2  117G  111G  609M  100% /
```

**٦٠٩ ميجا فاضية فقط.** لمّا تصل صفراً يتوقف البوت — لن يقدر يكتب
`seen-jobs.json` ولا `posted-jobs.json` ولا السجل.

البوت بريء: مجلده **٥٦ ميجا** كلها. المستهلكون: `~/Desktop` ٢٨ج ·
`~/.gradle` **١٣ج** (كاش بناء يُعاد توليده — حذفه يحرّر ١٣ج بلا خسارة).

---

## ١. ما يفعله البوت

يجمع وظائف سعودية من عدة مصادر كل ٣ ساعات، يفلترها على أربعة مجالات تقنية،
يتحقق منها بالذكاء الاصطناعي، وينشر المهم في قناة تيليجرام، ويرسل تقريراً
للأدمن بعد كل تشغيلة.

**قناة الوظائف:** `-1003334696675` · **قناة الأدمن:** `821043148`

---

## ٢. الجدولة

| | |
|---|---|
| التكرار | **كل ساعتين**: `00 · 02 · 04 · 06 · 08 · 10 · 12 · 14 · 16 · 18 · 20 · 22` بتوقيت الرياض (١٢ فرصة نشر يومياً — كانت ٨ كل ٣ ساعات) |
| الآلية | `cron` ينده كل ساعة (بدون تغيير)، و`run.sh` يخرج بصمت إلا في الساعات الاثنتي عشرة |
| زمن التشغيلة | ~١٥–٣٥ دقيقة (الشرائح أصغر من السابق) |
| المهلة القصوى | ٩٠ دقيقة (`JOBS_MAX_RUNTIME=5400`) ثم قتل قسري |

```cron
0 * * * * /home/panda/Desktop/Jobs-bot/run.sh >> /home/panda/Desktop/Jobs-bot/run.log 2>&1
@reboot sleep 120 && /home/panda/Desktop/Jobs-bot/run.sh >> /home/panda/Desktop/Jobs-bot/run.log 2>&1
```

> سطرا cron لم يتغيّرا إطلاقاً — التحديث تم داخل `RUN_HOURS` في `run.sh` فقط.

**ليش الساعات داخل `run.sh` لا في `cron`:** نسخة دبيان من cron ما تدعم توقيتاً
مستقلاً (لا `CRON_TZ` ولا توقيت لكل مستخدم)، وتوقيت الجهاز `America/New_York`
فيه توقيت صيفي والرياض ما عندها — فالساعات المحوّلة يدوياً كانت بتنزاح ساعة
كاملة مرّتين بالسنة. البوابة تقرأ `TZ=Asia/Riyadh` مباشرة فما تنزاح.

### الموعد إجباري — لا يُلغى

الموعد الفائت يظل **مستحقاً** ويُعوَّض بأول فرصة (جهاز مطفي · محمّل · قفل
محجوز · البوت طاح). **ما يُشطب إلا بعد تشغيلة ناجحة فعلاً** (`exit 0`).
المواعيد المشطوبة في `.run-slots` (آخر ٦٠ سطر).

- سقف التعويض: ٢٤ ساعة (`JOBS_CATCHUP_WINDOW`) — أقدم من ذلك لا يُعوَّض حتى ما
  نغرق القناة بإعلانات بايتة بعد انقطاع طويل.
- عدة مواعيد فائتة تُعوَّض بتشغيلة واحدة لا بتشغيلة لكل موعد.
- `JOBS_NOW_TEST=<epoch>` يزيّف "الآن" لفحص الجدولة بلا انتظار ساعات حقيقية.

---

## ٣. المجالات المستهدفة — أربعة + استثناءان

| المجال | نماذج المسمّيات |
|---|---|
| **QA/QC برمجيات** | QA Engineer · QA Analyst · SDET · Software Tester · Test Automation |
| **DevOps / SRE** | DevOps · DevSecOps · Development Operations · SRE · Platform/Infrastructure Engineer · CI/CD · Kubernetes · Terraform · Ansible · GitOps |
| **السحابة** | Cloud Engineer/Architect/Admin/Consultant · AWS · Azure · GCP · OpenShift · Solutions Architect |
| **الأمن السيبراني** | Cyber/InfoSec · Security Engineer/Analyst/Architect · SOC · Penetration Tester · Red/Blue/Purple Team · Ethical Hacking · VAPT · Threat Hunting/Intel · Incident Response · DFIR · Malware Analysis · Vulnerability Management · AppSec · Cloud Security · IAM · SIEM/SOAR · Security GRC |

### الاستثناءان (2026-08-18)

| الفئة | القبول | الوسم |
|---|---|---|
| **الوظائف الحكومية** | `is_government=true` (يثبته الـAI من نص الإعلان) | 🏛️ وظيفة حكومية |
| **فتح القبول والتسجيل** | جامعات (بكالوريوس · دبلوم · تجسير) · منح وابتعاث · تمهير · حملات عسكرية/مدنية وتجنيد | 📢 فتح قبول وتسجيل |

حوكمة القبول/التسجيل: المسمّى الصريح من أي مصدر، وعبارات الإعلان الرسمية من
الجهات المُراقَبة فقط. "بكالوريوس" مجرّدة لا تكفي. الاكتشاف: الجهات الـ٣٦ كاملة
على لنكدإن + استعلامات `ADMISSION_QUERIES` الثابتة في ياهو وwadhefa (بلا زيادة
على حد لنكدإن). صفحات الجامعات على لنكدإن "مدارس" authwall — ما تصلح لـ`f_C`.

### مستبعَد صراحةً

جودة الإنشاءات والمدني · الفحص الميداني وفحص المواقع · اللحام والـNDT · جودة
المصانع وخطوط الإنتاج والبترول والبتروكيماويات · جودة الأغذية والدواء
والمختبرات · السلامة المهنية (HSE) والمعايرة والمساحة · أنظمة الجودة المؤسسية
(ISO 9001 / QMS) · **الأمن المادي** (حراسة · دوريات · كاميرات مراقبة · سلامة من
الحريق) · إعلانات التدريب العامة من غير الجهات · أي وظيفة خارج الأربعة
والاستثناءين.

### قواعد دقيقة في `lib/sectors.js`

1. **الأمن المادي يُقتل أولاً** — "Security Officer" و"حارس أمن" يطابقان إشارة
   الأمن لكنهما ليسا سيبرانيين.
2. **DevOps · Cloud · Security: المسمّى وحده حاسم**، ولا يُسقطها مجال الشركة —
   مهندس أمن سيبراني في مصفاة وظيفته سيبرانية. (`reason: security-title` …)
3. **QA أشدّ** لأن "QA/QC Engineer" في السعودية غالباً إنشاءات:
   - مسمّى ميداني صريح → رفض فوري (`field-title`)
   - مسمّى برمجي صريح (`Software Test Engineer` · SDET · Test Automation) → قبول
     دائم حتى لو الوصف يذكر مجال الشركة (`software-qa-title`)
   - مسمّى عام (`QA Engineer`) → يمرّ إلا إذا الوصف ميداني صراحةً
     (`generic-title-field-body`). بلا وصف يمرّ، فما نخسر مصادر بلا وصف.
4. **تعمّدنا استبعاد كلمتَي `system` و`نظام` المجرّدتين** من إشارات البرمجيات —
   "quality management system" و"نظام إدارة الجودة" هما بالضبط جودة المصانع.
5. **لا تكتب `\bit\b`** في نمط السياق البرمجي: الـregex غير حسّاس للحالة فيطابق
   كلمة "it" الإنجليزية العادية في أي وصف.
6. **فتح القبول/التسجيل طبقتان** (`isAdmissionTitle` من أي مصدر بالمسمّى ·
   `isAdmissionAnnouncement` من الجهات بأي حقل) — والوسم `categoryForJob` مشتق
   محلياً حتماً لا من الـAI.

---

## ٤. النافذة الزمنية — ٢٤ ساعة متدحرجة

```
DATE_WINDOW_HOURS=24     # الوضع الفعّال
DATE_WINDOW_DAYS=1       # احتياطي لو صُفّرت الساعات
DROP_UNKNOWN_DATES=true  # إعلان بلا تاريخ يُرفض
```

**متدحرجة لا تقويمية**: "نُشرت خلال آخر ٢٤ ساعة" أياً كان موضع منتصف الليل.
التقويمية ("اليوم فقط") رفضت ٩ وظائف نُشرت قبل ساعات في تشغيلة ٠١:٠٠ لأن
"اليوم" بدأ للتو.

- المصادر ذات الوقت الدقيق (ISO بساعة) → فحص بالساعة.
- المصادر بتاريخ مجرّد (`2026-08-10`) → دقّة اليوم، تُقبل حتى `ceil(24/24)=1` يوم.
- **روافد المصادر مضبوطة على نفس المدى — غيّرها معها دائماً:**
  `LI_FRESH_TPR=r86400` · `NG_API_MAX_AGE_DAYS=1`.

كل تشغيلة تطبع توزيع أعمار المرفوضات:
`↳ أعمار المرفوضات زمنياً (النافذة = 1ي): 2ي:6 · 3ي:3 · 6ي:14 …`

---

## ٥. المصادر — بالترتيب الفعلي في المسار

| # | المصدر | الآلية | الحالة |
|---|---|---|---|
| ١ | مصادر API عامة | `sources/apiSources.js` | ٠/١١ نشطة (الباقي يحتاج مفاتيح) |
| ٢ | NaukriGulf | واجهة تطبيق الجوال `ngma.mobi/spapi/jobapi/search` | ✅ يعمل |
| ٣ | وظائف العرب | RSS + إثراء من صفحة الوظيفة | ✅ يعمل (تهدئة ٦ث) |
| ٤ | لوحات ATS | JSON عام لـ٧ منصّات | ✅ يعمل |
| ٥ | **LinkedIn** | واجهة الضيف `jobs-guest` | ✅ **المصدر الأقوى** |
| ٦ | **الجهات شبه الحكومية** | `f_C` لكل جهة | ✅ يعمل |
| ٧ | Yahoo Search | بحث HTTP + استخراج AI | ✅ ضعيف (بديل جوجل) |
| ٨ | wadhefa/ewdifh/sabbar | كشط HTML + استخراج AI | ✅ يعمل |
| — | Google | Puppeteer | 🔴 **مطفأ** — كابتشا (١٨٦ ملف `blocked-*.png`) |

### ٥.١ LinkedIn — نقطتان عامّتان

| النقطة | الاستخدام |
|---|---|
| `jobs-guest/jobs/api/seeMoreJobPostings/search` | قوائم الوظائف |
| `jobs-guest/jobs/api/jobPosting/{id}` | **الوصف الكامل** + معايير الوظيفة |

**العمق الحالي:** `LI_MAX_PAGES=4` (٤×١٠) · `LI_PER_KEYWORD=20` ·
`LI_FRESH_PER_KEYWORD=30` · **٣ تمريرات لكل كلمة**:

1. `f_TPR=r86400` — آخر ٢٤ ساعة (تُغذّي القناة فعلاً)
2. بلا فلتر — تغطية أوسع
3. **مدينة تدور بالساعة** من `Riyadh,Jeddah,Dammam,Khobar,Mecca`

لِنكدإن يرتّب بالصلة لا بالتاريخ، فالإعلانات الطازجة تُدفن في التمريرة العامة —
لذلك تمريرة `f_TPR` ضرورية. والبحث بالمدينة يُظهر إعلانات يدفنها البحث العام.

**الحصيلة المقيسة:** ٧٤ وظيفة → **٢٣١** بعد التعميق.

**إثراء الوصف** (`LI_DETAILS=true` · `LI_DETAILS_MAX=40`): كرت البحث بلا وصف
إطلاقاً، والقاعدة ترفض المسمّى العام ما لم تجد سياقاً برمجياً. الإثراء يقتصر على
الكروت ذات إشارة النطاق، بتهدئة ٦٠٠–١٢٠٠ms. انخفض السقف من ٦٠ مع الجدول
الساعتيني حتى يبقى الحِمل اليومي الأقصى على نقطة `jobPosting` نفسه (12×40 = 8×60).

#### مصايد وقعنا فيها — لا تكررها

- **`job_id` من ذيل الرابط، لا من `data-entity-urn` داخل الكتلة.** الـ`urn` يسبق
  الرابط في الـHTML، فكتلة تبدأ عند الرابط تلتقط `urn` الكرت **التالي** — كان
  يعلّق وصف وظيفة على وظيفة ثانية.
- **لا تحقن `Industries` (قطاع الشركة) في الوصف.** وسم على مستوى الشركة ومضلّل:
  KBR (هندسة مصانع) موسومة `IT Services and IT Consulting` فمرّرت QA/QC مصانع.

### ٥.٢ الجهات شبه الحكومية — `entities.json`

**٣٤ جهة محلولة من ٣٦.** ملف: `sources/entityJobs.js`

**ليش مصدر منفصل:** الجهات تنشر دورياً برامج خريجين وتدريب وابتعاث بمسمّيات
بلا كلمة تقنية، فمسار الكلمات يفوّتها. هنا نراقب **الجهة نفسها**.

**الآلية:** صفحة الشركة العامة تكشف `urn:li:organization:<id>` بلا تسجيل دخول →
`search?f_C=<id>&location=Saudi Arabia`.

> **لا تبحث باسم الجهة** — قِسناه فكان ضجيجاً: "SDAIA" أرجعت KAUST وAmadeus،
> و"Qiddiya" أرجعت شركات لا علاقة لها. البحث النصي يطابق *محتوى الإعلان* لا
> الشركة. أما `f_C` فدقيق ١٠٠٪.

> ⚠️ **منشورات لنكدإن (posts) غير متاحة** — `/company/<slug>/posts/` يرجع 200
> لكنه authwall. المتاح الوظائف فقط، وهي تغطي إعلانات التوظيف والبرامج.

**الجهات:** NHC · روشن · نيوم · البحر الأحمر · القدية · الدرعية · سدايا ·
الهيئة الوطنية للأمن السيبراني · علم · ثقة · تحكم · تكامل · منشآت · البريد
السعودي · أرامكو · سابك · معادن · السعودية للطاقة · stc · سيرار من stc · حلول
من stc · صندوق الاستثمارات العامة · طيران الرياض · الخطوط السعودية · علات ·
سير · التعاونية · ساما · هيئة السوق المالية · مدن · SIRC · المدفوعات السعودية ·
موبايلي · زين السعودية.

**غير محلولتين:** NUPCO · SAR — ما لقينا صفحتيهما.

**⚠️ تحقّق من `linkedin_name` بعد كل إضافة** — الـslug الخطأ يحلّ لجهة أخرى
تماماً. حالات حقيقية وقعت:

| بدا صحيحاً | الحقيقة |
|---|---|
| `sirc` | شركة أمريكية: Software Information Resource Corp |
| `national-housing-company` | صفحة وهمية باسم "N" بـ٢٤٦ متابعاً — لا NHC (الصحيحة `national-housing-company-nhc`) |
| `sdaia` | غير موجودة — الصحيحة `sdaia-ksa` |

**الصيانة:**
```bash
node resolve-entities.mjs            # يحلّ الناقص فقط
node resolve-entities.mjs --recheck  # يعيد التحقق من الكل
```

---

## ٦. التدوير — الحماية من الحظر

التشغيل كل ساعتين (١٢ مرة يومياً) كان سيرفع الطلبات ٥٠٪ لو بقيت الشرائح كما هي،
و**429 ظهر تاريخياً** حتى بثلاث تشغيلات كاملة. الحل ليس تقليل التشغيلات بل تقليل
ما تبحثه كل تشغيلة — فيبقى الحِمل اليومي على لنكدإن **مطابقاً تماماً** للمستوى
المجرَّب الآمن:

| المدوَّر | الشريحة | الدورة الكاملة |
|---|---|---|
| الكلمات المفتاحية (`KEYWORD_SLICE=8`) | ٨ من ٣٦ | ١٠ ساعات |
| الجهات (`ENTITY_SLICE=6`) | ٦ من ٣٤ | ١٢ ساعة |
| مدن لِنكدإن (`LI_CITIES`) | ١ من ٥ | ١٠ ساعات |

| | قبل (٨ تشغيلات كل ٣س) | بعد (١٢ تشغيلة كل ٢س) |
|---|---|---|
| استدعاءات لِنكدإن/يوم | ٩٦ | **٩٦** (نفسه) |
| فرص النشر/يوم | ٨ | **١٢** |
| تغطية الكلمات | ٣٦/٣٦ كل ٩ ساعات | **٣٦/٣٦ كل ١٠ ساعات** |

**التدوير مشتق من ساعة الرياض لا من عدّاد محفوظ** — بلا حالة، ويصحّ حتى لو فاتت
تشغيلة. `KEYWORD_SLICE=0` يعطّله. تمريرة الطازج `f_TPR=r86400` تضمن عدم ضياع
إعلان: أي إعلان يُلتقط خلال ≤١٠ ساعات من نشره وهو داخل نافذة ٢٤ ساعة دائماً.

**طبقة ثانية:** عند `429` انتظار `LI_BACKOFF_MS=25000` وإعادة محاولة واحدة بدل
خسارة الكلمة، وتهدئة ١.١–١.٨ث بين الطلبات، والإثراء يواصل بعد الانتظار بدل أن
يستسلم (بحدّ تراجعين).

### قاطع دوائر لنكدإن — `lib/liGate.js`

طبقتان إضافيتان (مشتركتان بين مصدر الكلمات ومصدر الجهات) حتى يكون حظر لنكدإن
المؤقت **غير مؤثّر إطلاقاً** في جلب الوظائف الجديدة:

| الطبقة | السلوك |
|---|---|
| قاطع داخل التشغيلة | ٤ استجابات 429 صعبة (`LI_RL_MAX_HITS`) ⇒ إيقاف فوري لكل عمل لنكدإن في التشغيلة، والباقي يكمل — نوفر وقت التشغيلة (كل تراجع = ٢٥ث) ولا نطيل أمد الحظر |
| تهدئة عبر التشغيلات | يُكتب موعد الانتهاء في `.li-cooldown` (`LI_RL_COOLDOWN_MS`=٤٥د)؛ أي تشغيلة تبدأ قبلها تتخطّى لنكدإن والجهات كلياً (تُسجَّل في السجل 🚦) — تمنع تشغيلات التعويض المتتالية من مطرقة API مكتوم |

العزل الأصلي قائم: كل مصدر مُغلَّف بـ`try/catch` في المسار الرئيسي، ففشل لنكدإن
لا يسقط تشغيلة ولا يمنع النشر من المصادر الأخرى. النتيجة: حتى لو لنكدإن محظور
تماماً، يستمر البوت في الجلب والنشر من كل المصادر الأخرى بنفس التشغيلة، ويعود
لنكدإن تلقائياً أول تشغيلة بعد انتهاء التهدئة.

---

## ٧. مسار المعالجة — مرحلة بمرحلة

```
١) جمع من المصادر الثمانية + استعلامات فتح القبول → ~٥٠٠–٧٠٠ وظيفة
٢) فلترة "سعودية + الأربعة + الاستثناءان"          → ~٦٠–١٧٠
٣) استخراج AI للروابط (سقف ٤٠ رابط، توازي ٣)
٤) فلترة ثانية بعد الاستخراج
٥) فلتر زمني + منع تكرار (داخل التشغيلة وعبرها)
٦) بوابة AI (دفعات ٢٥) — ترفض ما خرج عن النطاق التقني
٧) اختيار "أهم الوظائف" + كتابة وصف المسؤوليات (سقف ١٠)
٨) نشر تيليجرام + تقرير الأدمن
```

### نطاق القبول الموحّد — `inTargetScope()`

المجالات الأربعة لكل المصادر، **بالإضافة إلى**:
- **فتح القبول/التسجيل** — مسمّى صريح من أي مصدر، وعبارة إعلان رسمية من الجهات.
- **الوظائف الحكومية** — `is_government` يُثبته الـAI من نص الإعلان.
- **برامج الجهات** — برامج الخريجين والتدريب والابتعاث من الجهات المُراقَبة وحدها
  (`source === 'linkedin-entity'`). التقييد مقصود: مسمّيات البرامج بلا إشارة
  تقنية، فقبولها من أي مصدر كان بيغرق القناة.

هذه الفئات تُوسم (`is_admission` · `is_program` · `is_government`) و**تتخطّى
بوابة الذكاء الاصطناعي** لأن موجّهها يقبل المجالات الأربعة فقط فكان بيرفضها
حتماً. تظهر في الرسالة بوسم مشتق محلياً: `🏛️ وظيفة حكومية` · `📢 فتح قبول
وتسجيل — <الجهة>` · `🎓 برنامج توظيف/تدريب — <الجهة>`.

### منع التكرار — طبقتان

| الملف | المفتاح | الحجم الحالي |
|---|---|---|
| `seen-jobs.json` | الرابط + بصمة (المسمّى\|الشركة\|الموقع) | ١٣٠٠ مفتاح |
| `posted-jobs.json` | الرابط المُرسَل فعلاً (آخر ٣٠٠٠) | ٣٣٠ رابط |

---

## ٨. الذكاء الاصطناعي

```
NVIDIA_MODEL=z-ai/glm-5.2
NVIDIA_TIMEOUT_MS=90000
NVIDIA_MIN_GAP_MS=7000    # ~٨.٥ طلب/دقيقة
NVIDIA_MAX_TOKENS=4096
```

**حد المعدل مقيس تجريبياً:** بالضبط ١٠ طلبات تنجح ثم 429 مستمر حتى بفاصل ٢.٥ث —
فالحد الفعلي أقل من ~٢٤ طلب/دقيقة. الفاصل ٧ ثوانٍ كهامش أمان، عبر **طابور عام**
يرسل الطلبات واحداً واحداً بغض النظر عن `MAX_AI_PARALLEL`.

**ثلاثة استخدامات:** استخراج الوظيفة من HTML · بوابة التحقق من النطاق (دفعات
٢٥) · اختيار أهم ١٠ وظائف **وكتابة رسائلها** (المسمّى المنقّى + وصف المسؤوليات
بضوابط الحوكمة).

### إصلاح حديث مهم

`ECONNABORTED` (مهلة axios نفسها) **لم يكن مدرجاً** ضمن الأخطاء العابرة، فكل
مهلة تخرج من أول محاولة بلا إعادة — **١٠٧ فشلاً في يوم واحد، صفر إعادة**.
بعد الإصلاح: **١٨ إعادة محاولة** في تشغيلة واحدة.

وحمولة استدعاء "أهم الوظائف" صارت منحّفة (الحقول اللازمة + وصف ٣٠٠ حرف) بعد ما
ضخّمها إثراء وصف لِنكدإن.

### سياسة الفشل

فشل بوابة التحقق → نُبقي نتيجة الفلتر النصي (لا نرمي التشغيلة).
فشل اختيار الأهم → مسار بديل محلي بلا AI (`lib/importantFallback.js`).

---

## ٩. رسالة تيليجرام

```
💼 Senior Cloud Security Engineer
🏢 SiFi
📍 Riyadh, Saudi Arabia
🕒 Full-time

مسؤوليات تشمل تصميم وتنفيذ ضوابط الأمن السحابي على AWS وAzure، ومتابعة
الامتثال وتقييم المخاطر.

📌 الأمن السيبراني

🔗 https://sa.linkedin.com/jobs/view/...
```

**~٣٦٠ حرفاً.** كان سطر `⭐ …` يبرّر الاختيار ويقرأ كقائمة معايير — حُذف
(2026-08-18) وحلّ محله **وسم فئة** مشتق محلياً من بيانات الوظيفة لا من الـAI
(`📌 الأمن السيبراني` · `🏛️ وظيفة حكومية` · `📢 فتح قبول وتسجيل — NHC` ·
`🎓 برنامج توظيف/تدريب — NEOM`).

**حوكمة الرسالة** (`AGG_JOB_PROMPT` + تنقية في المسار):
- الـAI يكتب **المسمّى المنقّى** و**وصف المسؤوليات والمهام** فقط (جملتان إلى
  ثلاث، فصحى مختصرة، بلا حشو ولا إيموجي ولا Markdown ولا تلفيق مواعيد/شروط/
  رواتب). بدون وصف أصلي يشتق العام من المسمّى والفئة.
- **كل الحقائق** (شركة · موقع · دوام · راتب · رابط) من البيانات الأصلية حتماً —
  نسخة الـAI تُتجاهل (سبق وربط رابط وظيفة بعنوان ثانية).
- تعطّل الـAI → قوالب وصف محلية حتمية حسب الفئة + اختيار محلي.

الرابط يفضّل `apply_url` (التقديم المباشر) على رابط الإعلان.

---

## ١٠. قناة الأدمن — `821043148`

منفصلة عن قناة الوظائف: الأدمن يشوف التشخيص، والقناة تشوف الوظائف فقط.

**تقرير بعد كل تشغيلة** (حتى لو ٠ مُرسَل): العدد · التاريخ والوقت بتوقيت الرياض
· قائمة الوظائف · أرقام المراحل · ثم **المعايير السبعة**.

> **قرار مقصود: التقرير لا يذكر الكلمات المفتاحية إطلاقاً** — المعايير فقط. فيه
> اختبار يتحقق من عدم تسرّب أي كلمة من `keywords.json`.

**تنبيه عند أي عطل** — تغطية بطبقتين:

| الحدث | المصدر |
|---|---|
| تشغيلة أرسلت / لم ترسل | `lib/adminNotify.js` |
| لا نتائج من أي مصدر | `lib/adminNotify.js` |
| انهيار Node (`unhandledRejection` / `uncaughtException`) | `lib/adminNotify.js` |
| `keywords.json` تالف | `lib/adminNotify.js` |
| **المهلة تقتل التشغيلة** | `run.sh` عبر `curl` |
| **Node غير موجود** | `run.sh` عبر `curl` |
| **موعد عالق ٦ ساعات+** | `run.sh` عبر `curl` |

الطبقة الثانية ضرورية: الأعطال قبل بدء Node لا تصل عبر الوحدة لأن الكود ما اشتغل
أصلاً. وأي فشل في التنبيه نفسه يُبتلع — **لا يجوز أن يُسقط تشغيلةً ناجحة**.

`ساعة ليست موعداً` = لا تشغيلة أصلاً فلا تقرير (وإلا ٢٤ رسالة يومياً بلا معنى).

---

## ١١. بنية الملفات

```
jobs-scraper-fixed.js      المسار الرئيسي
run.sh                     بوابة التوقيت · التعويض · القفل · المهلة · تدوير السجل
keywords.json              ٣٦ كلمة (تُدوَّر ٨ لكل تشغيلة)
entities.json              ٣٦ جهة · ٣٤ محلولة
resolve-entities.mjs       أداة حلّ معرّفات الجهات

lib/
  sectors.js               قاعدة المجالات الأربعة + البرامج + الاستبعادات
  dateWindow.js            النافذة المتدحرجة/التقويمية · تحليل التواريخ عربي/إنجليزي
  jobFilter.js             النافذة + منع التكرار + توزيع أعمار المرفوضات
  jobIdentity.js           بصمة الوظيفة لمنع التكرار
  seenStore.js             مخزن المُعالَج
  qaClassifier.js          بوابة AI (دفعات ٢٥)
  ai.js                    عميل NVIDIA · طابور · تراجع · إعادة محاولة
  telegram.js              نشر الوظائف + قصّ الوصف
  adminNotify.js           تقارير الأدمن + تنبيهات الأعطال
  importantFallback.js     اختيار محلي بلا AI
  applyLink.js             استخراج رابط التقديم المباشر
  queryBuilder.js          استعلامات ذكية (مسمّى + مدينة)
  saudi.js                 كشف الموقع السعودي

sources/
  linkedin.js              واجهة الضيف + إثراء الوصف + تدوير المدن
  entityJobs.js            مراقبة الجهات عبر f_C
  naukrigulfApi.js         واجهة تطبيق الجوال
  jobsArab.js              RSS + إثراء
  atsBoards.js             ٧ منصّات ATS
  apiSources.js            ١١ واجهة عامة
  yahooSearch.js           بحث HTTP
  saudiSites.js            wadhefa · ewdifh · sabbar
  naukrigulf.js            كشط بالمتصفح (غير مستخدم — كروميوم مطفأ)
lib/liGate.js              قاطع دوائر لنكدإن (429) — عزل الحظر المؤقت
```

**حالة:** `.run-slots` · `seen-jobs.json` · `posted-jobs.json` · `run.log`
(تدوير عند ٢٠ ميجا) · `jobs-YYYY-MM-DD.json` · `api-jobs-YYYY-MM-DD.json`

**الاعتماديات:** `axios@^1.7.9` · `dotenv@^16.4.7` · `p-limit@^6.2.0` ·
`puppeteer@^23.11.1` (غير مستخدم فعلياً — كروميوم مطفأ)

---

## ١٢. الأداء الفعلي

**اليوم (2026-08-12):** ٤ تشغيلات ناجحة، أرسلت `1 · 3 · 8` = **١٢ وظيفة**.

**نموذج تشغيلة (١٥:٥١):**
```
📦 583 وظيفة من المصادر → 125 داخل النطاق
🎯 125 → 0  (خارج النافذة: 99 | سبق إرسالها: 26)
```

**المتوقع الواقعي:** ٠–١٠ وظائف لكل تشغيلة، وأصفار متكررة ليلاً وفي عطلة نهاية
الأسبوع. سقف التشغيلة ١٠ وظائف. **البوت يرسل كل الموجود، لكنه لا يخلق وظائف لم
تُنشر.**

---

## ١٣. أعطال معروفة ونقاط ضعف

| المشكلة | الأثر | الحالة |
|---|---|---|
| **القرص ١٠٠٪ (٦٠٩م)** | البوت يتوقف عند الامتلاء | 🔴 **يحتاج تدخّلك** |
| Google محجوب بكابتشا | فقدنا أقوى مصدر روابط | مطفأ · ياهو بديل ضعيف |
| `jooble` يرد 403 | مصدر مفقود | غير مفعّل |
| مهلات NVIDIA | تخطّي روابط | خفّت كثيراً بعد الإصلاح |
| NUPCO · SAR | جهتان غير مراقَبتَين | تحتاج slug يدوياً |
| `bayt` · `indeed.sa` · `gulftalent` | 403 عبر Cloudflare | تحتاج بروكسي |
| ذاكرة الباي | كروميوم يسقط التشغيلة | حُلّ بإطفاء كروميوم |
| حظر لنكدإن المؤقت (429) | كان يبطّئ تشغيلة ويخسر كلمات | ✅ **محايد** — قاطع دوائر `lib/liGate.js` + تهدئة عبر التشغيلات |

### تنبيه تشغيلي

**أي تعديل على الكود أثناء تشغيلة جارية لا يدخل حيّز التنفيذ إلا في التشغيلة
التالية** — Node يحمّل الوحدات عند البدء. للتحقق فوراً: `FORCE_RUN=1 ./run.sh`.

---

## ١٤. أوامر سريعة

```bash
FORCE_RUN=1 ./run.sh                 # تشغيل يدوي فوري (يتجاوز بوابة التوقيت)
ENABLE_TELEGRAM=false FORCE_RUN=1 ./run.sh   # تجربة بلا نشر
node resolve-entities.mjs            # حلّ معرّفات الجهات الناقصة
tail -f run.log                      # متابعة حيّة
cat .run-slots                       # المواعيد المشطوبة
crontab -l                           # الجدول
flock -n /tmp/jobs-scraper.lock -c echo   # هل القفل حر؟
```

### مفاتيح الضبط الأكثر استخداماً

| المفتاح | القيمة | الأثر |
|---|---|---|
| `DATE_WINDOW_HOURS` | 24 | وسّعها لمزيد من الوظائف الأقدم |
| `KEYWORD_SLICE` | 8 | ارفعها لتغطية أسرع، وتزيد خطر 429 |
| `ENTITY_SLICE` | 6 | عدد الجهات لكل تشغيلة |
| `LI_MAX_PAGES` | 4 | عمق لِنكدإن |
| `JOBS_RUN_HOURS` | 00,02,…,22 | ساعات التشغيل (كل ساعتين) |
| `LI_RL_MAX_HITS` | 4 | عتبة 429 الصعبة قبل قطع لنكدإن بالتشغيلة |
| `LI_RL_COOLDOWN_MS` | 2700000 | مدة تهدئة لنكدإن عبر التشغيلات (٤٥د) |
| `ENABLE_ENTITIES` | true | تعطيل مراقبة الجهات |
